import com.raylib.Raylib;

import static com.raylib.Raylib.LoadSoundFromWave;
import static com.raylib.Raylib.LoadTexture;

/*public class Sound {
    public static void PlaySound(Sound sound)
    {
        PlaySound(Main.sound);
    }

    public static void LoadSound(int i)
    {
        Main.effect = LoadSound("resources/sounds/" + Define.lines.object[i] +".wav");
        Parser.soundLoaded = true;
    }
}*/
